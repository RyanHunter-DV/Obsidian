#python

# builtins

## split(sep)
split string to list, with separator.
```python
s = "hello world";
a = s.split(" ");
```
# string operations
- `+`, string concatenation
- 'H' in a

## string format
```python
s = "hello world";
d = 10
print("string %s, %d" % (s,d))
```